{
  "name": "getwordsandweather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./src/index.html",
    "rm": "rm -rf .cache/ dist/",
    "dev": "rm && npm start"  
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.30.0"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
